import tillageConfiguration from './tillageConfiguration';
import fertilizerConfiguration from './fertilizerConfiguration';
import plantingConfiguration from './plantingConfiguration';
import treesConfiguration from './treesConfiguration';
import ingredientsConfiguration from './ingredientsConfiguration';
import digestibilityConfiguration from './digestibilityConfiguration';
import nutritionConfiguration from './nutritionConfiguration';
import veterinaryConfiguration from './veterinaryConfiguration';
import grazingConfiguration from './grazingConfiguration';
import pastureConfiguration from './pastureConfiguration';
import otherConfiguration from './otherConfiguration';

const allPracticeConfigurations = [
	tillageConfiguration,
	fertilizerConfiguration,
	plantingConfiguration,
	treesConfiguration,
	ingredientsConfiguration,
	digestibilityConfiguration,
	nutritionConfiguration,
	veterinaryConfiguration,
	grazingConfiguration,
	pastureConfiguration,
	otherConfiguration,
];

export default allPracticeConfigurations;
